{
    "name": "litup-subgraph",
    "version": "0.1.0",
    "scripts": {
        "codegen": "graph codegen",
        "build": "graph build",
        "deploy:thegraph": "graph deploy --node https://api.studio.thegraph.com/deploy/ litup",
        "deploy:goldsky": "goldsky subgraph deploy litup/1.0.0 --path ."
    },
    "dependencies": {
        "@graphprotocol/graph-cli": "^0.98.1",
        "@graphprotocol/graph-ts": "^0.35.0"
    }
}